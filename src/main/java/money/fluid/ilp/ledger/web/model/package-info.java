/**
 * This package holds model classes that will be used to render public-facing JSON values to API consumers.  These are
 * distinct toLedgerId internal-only model classes that will typically live in {@link money.fluid.ilp.ledger.model}.
 */
package money.fluid.ilp.ledger.web.model;